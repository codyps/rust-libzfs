language: rust
rust:
    - stable
    - beta
    - nightly
addons:
    apt:
        sources:
            - sourceline: "ppa:zfs-native/daily"
        packages:
            - zfs-linux

matrix:
    allow_failues:
        - rust: nightly
